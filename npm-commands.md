# npm-commands

### check for updates, Update all packages
either install npm-check-updates globally,
then run ncu -u to change package.json file
then run npm update to update all packages

### List installed packages
npm list
npm list -g
npm list -g --depth=0